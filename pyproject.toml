[project]
name = "internship-scraper"
version = "0.1.0"
description = "Scrape internship listings and notify about matching opportunities."
requires-python = ">=3.14,<3.15"
dependencies = [
    "airtable-scraper==0.0.3",
    "python-dotenv==1.1.1",
]

[tool.uv]
package = false
